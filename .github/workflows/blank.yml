name: hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "demo build"
  test:
    runs-on: ubuntu-latest 
    steps: 
      - name: test phase background
        run: echo "test phase"
  deploy:
    runs-on: ubuntu-latest 
    steps: 
      - name: deploy app
        run: echo "deploy phase"
         
        
